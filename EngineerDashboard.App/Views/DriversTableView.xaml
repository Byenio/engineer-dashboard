<UserControl x:Class="EngineerDashboard.App.Views.DriversTableView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:views="clr-namespace:EngineerDashboard.App.Views"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d"
             d:DesignHeight="1000" d:DesignWidth="1600">
    <UserControl.Resources>
        <Style x:Key="NonInteractiveListView" TargetType="{x:Type ui:ListViewItem}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Margin" Value="0 2"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ui:ListViewItem}">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <FontFamily x:Key="JetBrainsMonoExtraBold">pack://application,,,/Assets/Fonts/#JetBrains Mono ExtraBold</FontFamily>
        
        <Style x:Key="HeaderContent" TargetType="{x:Type ui:TextBlock}">
            <Setter Property="FontFamily" Value="{StaticResource JetBrainsMonoExtraBold}" />
            <Setter Property="FontSize" Value="18" />
            <Setter Property="Margin" Value="4 0" />
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="20" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Height="20" Margin="80 0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="10" />
                <ColumnDefinition Width="50" />
                <ColumnDefinition Width="200" />
                <ColumnDefinition Width="60" />
                <ColumnDefinition Width="120" />
                <ColumnDefinition Width="120" />
                <ColumnDefinition Width="80" />
                <ColumnDefinition Width="60" />
                <ColumnDefinition Width="100" />
                <ColumnDefinition Width="80" />
                <ColumnDefinition Width="120" />
                <ColumnDefinition Width="120" />
                <ColumnDefinition Width="120" />
            </Grid.ColumnDefinitions>

            <ui:TextBlock Grid.Column="1" Text="#" Style="{StaticResource HeaderContent}" />
            <ui:TextBlock Grid.Column="2" Text="Driver" Style="{StaticResource HeaderContent}" />
            <ui:TextBlock Grid.Column="3" Text="DRS" Style="{StaticResource HeaderContent}" />
            <ui:TextBlock Grid.Column="4" Text="Interval" Style="{StaticResource HeaderContent}" TextAlignment="Right"/>
            <ui:TextBlock Grid.Column="5" Text="Leader" Style="{StaticResource HeaderContent}" TextAlignment="Right"/>
            <ui:TextBlock Grid.Column="6" Text="Tyre" Style="{StaticResource HeaderContent}" TextAlignment="Center"/>
            <ui:TextBlock Grid.Column="7" Text="Wear" Style="{StaticResource HeaderContent}" TextAlignment="Right"/>
            <ui:TextBlock Grid.Column="8" Text="Tyre Age" Style="{StaticResource HeaderContent}" TextAlignment="Right"/>
            <ui:TextBlock Grid.Column="9" Text="Pits" Style="{StaticResource HeaderContent}" TextAlignment="Center"/>
            <ui:TextBlock Grid.Column="10" Text="Sector 1" Style="{StaticResource HeaderContent}" TextAlignment="Right"/>
            <ui:TextBlock Grid.Column="11" Text="Sector 2" Style="{StaticResource HeaderContent}" TextAlignment="Right"/>
            <ui:TextBlock Grid.Column="12" Text="Last Lap" Style="{StaticResource HeaderContent}" TextAlignment="Right"/>
        </Grid>
        
        <ui:ListView Grid.Row="1"
                     ItemsSource="{Binding Drivers}" 
                     IsSynchronizedWithCurrentItem="True"
                     BorderThickness="0"
                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                     Margin="80 10"
                     ItemContainerStyle="{StaticResource NonInteractiveListView}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <views:DriversRowView />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ui:ListView>
    </Grid>
</UserControl>
