<UserControl x:Class="EngineerDashboard.App.Views.DamageCardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:EngineerDashboard.App.Views"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d"
             d:DesignHeight="Auto" d:DesignWidth="Auto">
    <UserControl.Resources>
        <FontFamily x:Key="JetBrainsMonoBold">pack://application,,,/Assets/Fonts/#JetBrains Mono Regular</FontFamily>
        
        <Style x:Key="Text" TargetType="{x:Type ui:TextBlock}">
            <Setter Property="FontFamily" Value="{StaticResource JetBrainsMonoBold}" />
            <Setter Property="FontSize" Value="18" />
            <Setter Property="Margin" Value="4 1" />
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="4*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <Grid Grid.Row="0" Grid.Column="0">
            <Polyline Points="0,60 200,60 200,0 160,0 0,40 0,60" VerticalAlignment="Bottom" HorizontalAlignment="Right"
                      Stroke="#121212" StrokeThickness="4" Fill="{Binding FrontLeftWingDamageColor}"/>
            <ui:TextBlock Text="{Binding FrontLeftWingDamageString}" Style="{StaticResource Text}" VerticalAlignment="Bottom" HorizontalAlignment="Right" Padding="30 20" />
        </Grid>

        <Grid Grid.Row="0" Grid.Column="1">
            <Polyline Points="200,60 0,60 0,0 50,0 200,40 200,60" VerticalAlignment="Bottom" HorizontalAlignment="Left"
                      Stroke="#121212" StrokeThickness="4" Fill="{Binding FrontRightWingDamageColor}"/>
            <ui:TextBlock Text="{Binding FrontRightWingDamageString}" Style="{StaticResource Text}" VerticalAlignment="Bottom" HorizontalAlignment="Left" Padding="30 20" />
        </Grid>
        
        <Grid Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2">
            <Polyline Points="80,440 80,420 0,180 80,160 100,60 100,0 200,0 200,60 220,160 300,180 220,420 220,440 80,440" VerticalAlignment="Center" HorizontalAlignment="Center"
                      Stroke="#121212" StrokeThickness="4" Fill="{Binding FloorDamageColor}"/>
            <ui:TextBlock Text="{Binding FloorDamageString}" Style="{StaticResource Text}" VerticalAlignment="Center" HorizontalAlignment="Center" />
        </Grid>
        
        <Grid Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2">
            <Polyline Points="0,60 30,0 230,0 260,60 0,60" VerticalAlignment="Top" HorizontalAlignment="Center"
                      Stroke="#121212" StrokeThickness="4" Fill="{Binding RearWingDamageColor}"/>
            <ui:TextBlock Text="{Binding RearWingDamageString}" Style="{StaticResource Text}" VerticalAlignment="Top" HorizontalAlignment="Center" Padding="0 20" />
        </Grid>
    </Grid>
</UserControl>
