<UserControl x:Class="EngineerDashboard.App.Views.SessionInfoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:EngineerDashboard.App.Views"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d"
             d:DesignHeight="60" d:DesignWidth="1600">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        
        <ui:TextBlock Text="{Binding SessionType}" Grid.Column="0" />
        <ui:TextBlock Text="{Binding TotalLaps}" Grid.Column="1" />
        <ui:TextBlock Text="{Binding TrackTemperature}" Grid.Column="2" />
        <ui:TextBlock Text="{Binding AirTemperature}" Grid.Column="3" />
    </Grid>
</UserControl>
